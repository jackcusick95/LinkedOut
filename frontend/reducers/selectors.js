// export const findLike = (likes, likeable_id, likeable_type, liker_id) => {
//     for (let id in likes) {
//         let like = likes[id];
//         if (
//             like.likeable_id === likeable_id &&
//             like.likeable_type === likeable_type &&
//             like.liker_id === liker_id
//         ) {
//             return like;
//         }
//     }
//     return false;
// }
